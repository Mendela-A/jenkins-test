#!groovy

properties([disableConcurrentBuilds()])

pipeline {
    agent { 
        label 'Built-In Node'
        }
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
    }
    stages {
        stage("First step") {
            steps {
                sh 'ssh root@172.16.0.41 \'hostname\''
            }
        }
    }
}
